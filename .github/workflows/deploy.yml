name: Deploy to Production (FTP)

on:
  workflow_dispatch:
    inputs:
      confirm:
        description: 'Type "deploy-prod" to confirm production deployment'
        required: true

jobs:
  deploy:
    if: github.event.inputs.confirm == 'deploy-prod'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install lftp
        run: sudo apt-get update && sudo apt-get install -y lftp

      - name: FTP Deploy (targeted)
        run: |
          lftp -u "$FTP_USER","$FTP_PASSWORD" "$FTP_HOST" <<'SCRIPT'
          set ssl:verify-certificate no
          set ftp:ssl-allow yes
          set ftp:ssl-protect-data yes
          set net:timeout 30
          set net:max-retries 3

          # 1. recruitment-graduate pages (main deliverable) -- overwrite to force update
          mirror --reverse --overwrite --verbose ./site/recruitment-graduate/ www/recruitment-graduate/

          # 1b. Corporate page (T-344)
          mirror --reverse --overwrite --verbose ./site/corporate/ www/corporate/

          # 1c. Experienced recruitment page (T-348)
          mirror --reverse --overwrite --verbose ./site/recruitment-experienced/ www/recruitment-experienced/

          # 2. New CSS/JS added for recruitment pages
          mkdir -p www/css/common/
          put ./site/css/common/animations.css -o www/css/common/animations.css
          mkdir -p www/scripts/common/
          put ./site/scripts/common/scroll-reveal.js -o www/scripts/common/scroll-reveal.js

          quit
          SCRIPT
        env:
          FTP_HOST: ${{ secrets.FTP_HOST }}
          FTP_USER: ${{ secrets.FTP_USER }}
          FTP_PASSWORD: ${{ secrets.FTP_PASSWORD }}
